import messageModel from "../../DB/models/message.model.js";




// export const sendMsg = async (req, res, next) => {

//     const { from, to, body } = req.body
//     if (from) {
//         const sender = await UserModel.findById(from)
//         if (!sender) {
//             return next(new Error("sender not found", { cause: 404 }));
//         }

//     }

//     const receiver = await UserModel.findById(to)
//     if (!receiver) {
//         return next(new Error("receiver not found", { cause: 404 }));
//     }


//     if (req.files.length > 0) {
//         const paths = []
//         for (const file of req.files) {
//             paths.push(file.path)

//         }

     
// await uploadMultiFiles (paths,dest:`/messagaes/${receiver._id}  `)



//     }














// }





